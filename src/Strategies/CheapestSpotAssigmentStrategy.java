package Strategies;

import enums.VehicalType;
import model.Gate;
import model.ParkingSpot;

public class CheapestSpotAssigmentStrategy implements SpotAssigmentStrategy{
    @Override
    public ParkingSpot assignSpot(VehicalType vehicalType, Gate gate) {
        return null;
    }
}
